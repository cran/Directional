#useDynLib(Directional, .registration = TRUE)

importFrom("abind", abind)
importFrom("MASS", mvrnorm)
importFrom("graphics", "contour", "lines", "plot", "points")
importFrom("stats", "aggregate", "coef", "cor", "dist", "dnorm",
            "kmeans", "lm", "optim", "optimize", "pchisq", "pf",
            "pnorm", "qchisq", "quantile", "rbeta", "rnorm", "runif",
            "sd", "uniroot")
import("doParallel")
import("parallel")
import("foreach")

exportPattern("^[[:alpha:]]+")
exportPattern("^[^\\.]")
